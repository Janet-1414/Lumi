export { useToast } from '@/components/ui/Toast'
/**
 * hooks/useToast.ts
 *
 * Re-exports useToast from the Toast component.
 * Gives hooks/ a single consistent import path.
 *
 * Usage:
 *   import { useToast } from '@/hooks/useToast'
 *   const { success, error, warning } = useToast()
 */

export { useToast } from '@/components/ui/Toast'
